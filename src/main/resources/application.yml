spring:
  application:
    name: user-service
  config:
    import: "optional:configserver:https://configserver-microservice.onrender.com"
  cloud:
    config:
      enabled: true
      fail-fast: true





